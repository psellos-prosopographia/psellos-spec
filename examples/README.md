This example shows the smallest valid dataset for the v0.1.0 minimal person + parent relationship
spec, with two people, one `parent_of` assertion, and an explicit Psellos extension namespace stub.
Validate `examples/minimal.person-parent.v0.1.json` against
`schema/minimal.person-parent.v0.1.json`.
